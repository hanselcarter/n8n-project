# Copy this file to .env if you want to override defaults.
# This file holds n8n infrastructure config only — NOT credentials.
# Credentials (Gemini API key, Gmail OAuth, Slack webhook) are configured
# inside n8n via the UI after importing the workflow.

# Set to true if you're serving n8n over HTTPS (with a reverse proxy)
N8N_SECURE_COOKIE=false
